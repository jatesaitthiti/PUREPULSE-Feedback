# ไฟล์ feedback ดิบ (มีข้อมูลส่วนตัวของผู้ทดสอบ — อย่า commit ขึ้น GitHub)
# ignore ทั้งโฟลเดอร์รวมโฟลเดอร์ย่อย (เช่น Feedback/Feedback3/)
# ห้ามเปลี่ยนกลับเป็น pattern รายนามสกุล — `Feedback/*.jpg` ไม่ครอบไฟล์ในโฟลเดอร์ย่อย
Feedback/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code (เก็บ commands ไว้ใน repo ได้)
# .claude/

# Node (ถ้ามี)
node_modules/
dist/
*.local

# Python (ถ้ามี)
__pycache__/
*.pyc
.venv/
